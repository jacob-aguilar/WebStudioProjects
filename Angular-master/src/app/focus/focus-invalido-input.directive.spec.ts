import { FocusInvalidoInputDirective } from './focus-invalido-input.directive';

describe('FocusInvalidoInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusInvalidoInputDirective();
    expect(directive).toBeTruthy();
  });
});
