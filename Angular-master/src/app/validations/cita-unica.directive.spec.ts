import { CitaUnicaDirective } from './cita-unica.directive';

describe('CitaUnicaDirective', () => {
  it('should create an instance', () => {
    const directive = new CitaUnicaDirective();
    expect(directive).toBeTruthy();
  });
});
