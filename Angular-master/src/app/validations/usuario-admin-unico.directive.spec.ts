import { UsuarioAdminUnicoDirective } from './usuario-admin-unico.directive';

describe('UsuarioAdminUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new UsuarioAdminUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
