import { FechaUnicaDirective } from './fecha-unica.directive';

describe('FechaUnicaDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaUnicaDirective();
    expect(directive).toBeTruthy();
  });
});
