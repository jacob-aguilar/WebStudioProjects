import { IdentidadAdminUnicaDirective } from './identidadAdmin-unica.directive';

describe('IndetidadAdminUnicaDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentidadAdminUnicaDirective();
    expect(directive).toBeTruthy();
  });
});
