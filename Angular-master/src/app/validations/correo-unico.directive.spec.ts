import { CorreoUnicoDirective } from './correo-unico.directive';

describe('CorreoUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new CorreoUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
