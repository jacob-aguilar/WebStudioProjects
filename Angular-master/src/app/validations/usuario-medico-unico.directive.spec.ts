import { UsuarioMedicoUnicoDirective } from './usuario-medico-unico.directive';

describe('UsuarioMedicoUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new UsuarioMedicoUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
