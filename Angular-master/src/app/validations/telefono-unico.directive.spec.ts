import { TelefonoUnicoDirective } from './telefono-unico.directive';

describe('TelefonoUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefonoUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
