import { CuentaUnicaDirective } from './cuenta-unica.directive';

describe('CuentaUnicaDirective', () => {
  it('should create an instance', () => {
    const directive = new CuentaUnicaDirective();
    expect(directive).toBeTruthy();
  });
});
