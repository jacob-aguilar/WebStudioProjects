import { IdentidadUnicaDirective } from './identidad-unica.directive';

describe('IdentidadUnicaDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentidadUnicaDirective();
    expect(directive).toBeTruthy();
  });
});
